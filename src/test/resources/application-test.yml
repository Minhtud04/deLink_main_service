# ===================================================================
# TEST-ONLY CONFIGURATION
# - This file is automatically loaded when the 'test' profile is active.
# - It provides mock values for all required properties.
# ===================================================================
server:
  port: 8081
  address: 0.0.0.0
  servlet:
    context-path: /api/v1
    session:
      cookie:
        same-site: none
        secure: true


spring:
  application:
      name: delink

  datasource:
    url: jdbc:postgresql://localhost:5432/deLink_postgres
    username: minhtud04
    password: Harmony_love04.,
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update                  #for local config only
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  session:
    store-type: redis
  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    aws:
      region:
        static: us-east-1